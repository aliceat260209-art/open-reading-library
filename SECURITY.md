# 安全与隐私

请不要在公开 Issue 中披露漏洞、用户文件、账号信息、访问令牌或可复现的私有阅读内容。

在项目公开发布前，请通过仓库维护者的私下联系方式报告安全问题；发布后将提供正式的安全邮箱与响应 SLA。

## 高风险区域

- 文件上传、格式解析、EPUB/MOBI 解压与 OCR；
- 多租户权限校验与对象存储 URL；
- AI 任务队列、Webhook 与账单额度；
- 摘录、笔记与全文检索的访问控制；
- 日志、备份和删除流程中的个人数据。

任何部署都必须将上传文件视为不可信输入，使用隔离的异步 worker 处理，并限制文件类型、体积、解压后大小、CPU 时间和网络访问。